apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: # FILL THIS OUT BEFORE DEPLOYING
commonLabels:
  app: llm-load-test-exporter

resources:
  - deployment.yaml
  - service.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - servicemonitor.yaml

configMapGenerator:
  - name: observability-metrics-custom-allowlist
    options:
      disableNameSuffixHash: true
    files:
    - files/uwl_metrics_list.yaml
